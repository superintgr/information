# information

I : "information"
i : "copy"
S : "substrate"
x : "variable"
X : "state"

def computation(substrate, medium):
    """
    Defines a function `C[permutation](S)` on substrate `S` of at least two possible state `x` in `X`, whose transformation consists of applying permutation to each `x` in substrate `S` where `x` -> `permutation(x)`
    
